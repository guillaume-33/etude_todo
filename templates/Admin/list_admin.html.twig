{% extends'base.html.twig' %}

{% block main %}
    <h1>Les administrateurs</h1>
    {% for admin in admins %}
        {%  if 'ROLE_ADMIN' in admin.roles %}
            <article>
                <div>
                    <p>Nom : {{ admin.nom }}</p>
                    <p>Prénom : {{ admin.prenom }}</p>
                    <p>Mail : {{ admin.email }}</p>
                    <a href="#">Contacter cet administrateur</a>
                    <a href="{{ path('update_admin', {'id':admin.id}) }}">Retirer le statut d'administrateur</a>
                </div>
            </article>
        {% endif %}
    {% endfor %}
{% endblock %}